import { CircleCheck, Clock3 } from "lucide-react";
import { DEMO_PRODUCTS } from "@/lib/catalog";

export default function OperatorOrdersPage() { return <div className="space-y-8"><div className="border-b border-ink pb-6"><p className="eyebrow text-muted">OPERATOR / ORDERS</p><h1 className="mt-3 text-4xl font-black tracking-[-.08em]">입금·주문 처리</h1><p className="mt-3 text-sm text-muted">내 숍 상품에 대한 결제 상태만 표시됩니다.</p></div><div className="grid gap-4 md:grid-cols-3"><div className="border border-line p-5"><Clock3 size={17} /><p className="mt-7 text-xs text-muted">입금 확인 대기</p><p className="mt-2 font-mono text-3xl font-bold">03</p></div><div className="border border-line p-5"><CircleCheck size={17} /><p className="mt-7 text-xs text-muted">오늘 확인 완료</p><p className="mt-2 font-mono text-3xl font-bold">12</p></div><div className="border border-line bg-ink p-5 text-paper"><p className="eyebrow text-zinc-400">THIS MONTH</p><p className="mt-7 font-mono text-3xl font-bold">1,248,000원</p><p className="mt-2 text-xs text-zinc-400">내 숍 판매 금액</p></div></div><div className="divide-y divide-line border-y border-line">{DEMO_PRODUCTS.slice(0, 4).map((product, index) => <div className="flex flex-col justify-between gap-4 py-5 md:flex-row md:items-center" key={product.id}><div className="flex items-center gap-4"><img alt="" className="size-16 object-cover" src={product.imageUrls[0]} /><div><p className="text-sm font-bold">{product.title}</p><p className="mt-1 text-xs text-muted">주문 NN-20260719-00{index + 1} · 구매자 member****</p></div></div><div className="flex items-center gap-6"><span className="text-right"><span className="block text-[10px] text-muted">결제 금액</span><strong className="font-mono text-sm">{product.price.toLocaleString("ko-KR")}원</strong></span><button className="h-10 border border-ink px-4 text-xs font-bold">입금 확인</button></div></div>)}</div></div>; }

