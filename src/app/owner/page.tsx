import Link from "next/link";
import { ArrowUpRight, Database, ShieldCheck, Store } from "lucide-react";
import { DEMO_STORES } from "@/lib/catalog";

export default function OwnerPage() { return <div className="space-y-10"><div className="flex items-end justify-between border-b border-ink pb-7"><div><p className="eyebrow text-muted">OWNER / ALL STORES</p><h1 className="mt-3 text-4xl font-black tracking-[-.08em]">소유자 센터</h1><p className="mt-3 text-sm text-muted">모든 숍과 운영 데이터를 한 곳에서 확인합니다.</p></div><span className="flex items-center gap-2 border border-emerald-200 bg-emerald-50 px-3 py-2 text-[10px] font-bold text-emerald-800"><ShieldCheck size={13} /> OWNER ACCESS</span></div><div className="grid gap-px border border-line bg-line md:grid-cols-3"><div className="bg-paper p-6"><Store size={17} /><p className="mt-8 text-xs text-muted">운영 중인 숍</p><p className="mt-2 font-mono text-3xl font-bold">03</p></div><div className="bg-paper p-6"><Database size={17} /><p className="mt-8 text-xs text-muted">이번 달 총 거래</p><p className="mt-2 font-mono text-3xl font-bold">4,820,000원</p></div><div className="bg-ink p-6 text-paper"><p className="eyebrow text-zinc-400">AUDIT / EVENTS</p><p className="mt-8 font-mono text-3xl font-bold">184</p><p className="mt-2 text-xs text-zinc-400">최근 30일 기록</p></div></div><section><div className="mb-4 border-b border-ink pb-4"><p className="eyebrow text-muted">STORE OPERATORS</p><h2 className="mt-2 text-xl font-black">숍별 운영 현황</h2></div><div className="grid gap-3 md:grid-cols-3">{DEMO_STORES.map((store) => <Link className="border border-line p-5 transition-transform hover:-translate-y-1" href={`/stores/${store.slug}`} key={store.id} style={{ borderTopColor: store.accent, borderTopWidth: 4 }}><p className="text-xs font-bold">{store.name}</p><p className="mt-2 text-[11px] text-muted">운영자 {store.operator}</p><div className="mt-8 flex items-end justify-between"><span className="font-mono text-2xl font-bold">{store.id === "store-archive" ? "16" : store.id === "store-north" ? "21" : "12"}<small className="ml-1 text-xs font-sans font-normal text-muted">items</small></span><ArrowUpRight size={16} /></div></Link>)}</div></section></div>; }

